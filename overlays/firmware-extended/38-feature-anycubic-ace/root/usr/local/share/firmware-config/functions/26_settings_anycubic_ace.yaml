settings:
  snapmaker_components:
    items:
      anycubic_ace:
        label: Anycubic ACE
        description: Optional Anycubic ACE Pro / ACE 2 Pro storage, feed assist, dryer, and status integration.
        help_url: http://snapmakeru1-extended-firmware.pages.dev/anycubic_ace
        get_cmd:
          - bash
          - -c
          - test -f /oem/printer_data/config/extended/klipper/ace.cfg && echo "enabled" || echo "disabled"
        options:
          enabled:
            label: Enabled
            confirm: |
              Enable Anycubic ACE Pro / ACE 2 Pro support?

              This creates ace.cfg and restarts Klipper. The ACE model is auto-detected via USB — no manual serial path needed. Tune the slot feed/load/retract lengths for your tube routing before using movement commands.
            cmd:
              - bash
              - -ec
              - |
                ace_found=0
                for ace_device in /dev/serial/by-id/usb-ANYCUBIC* /dev/serial/by-id/usb-1a86_USB_Single_Serial_* /dev/serial/by-id/usb-1a86_USB_Serial*; do
                  if [ -e "$ace_device" ]; then
                    ace_found=1
                    break
                  fi
                done
                if [ "$ace_found" -ne 1 ]; then
                  echo "ERROR: No Anycubic ACE detected on USB. Connect the ACE and try again."
                  exit 1
                fi
                mkdir -p /oem/printer_data/config/extended/klipper
                cp -n /usr/local/share/firmware-config/tweaks/klipper/ace.cfg /oem/printer_data/config/extended/klipper/ace.cfg
                chown lava:lava /oem/printer_data/config/extended/klipper/ace.cfg
                echo "Anycubic ACE enabled. Review /oem/printer_data/config/extended/klipper/ace.cfg and tune slot lengths. Restarting Klipper..."
                /etc/init.d/S60klipper restart
          disabled:
            label: Disabled
            cmd:
              - bash
              - -ec
              - |
                rm -f /oem/printer_data/config/extended/klipper/ace.cfg
                if [ "$(/usr/local/bin/extended-config.py get /oem/printer_data/config/extended/extended2.cfg components rfid_hardware 2>/dev/null || true)" = "ace" ]; then
                  /usr/local/bin/extended-config.py add /oem/printer_data/config/extended/extended2.cfg components rfid_hardware snapmaker
                fi
                if [ "$(/usr/local/bin/extended-config.py get /oem/printer_data/config/extended/extended2.cfg components rfid_software 2>/dev/null || true)" = "ace" ]; then
                  /usr/local/bin/extended-config.py add /oem/printer_data/config/extended/extended2.cfg components rfid_software snapmaker
                fi
                echo "Anycubic ACE disabled. Restarting Klipper..."
                /etc/init.d/S59rfid-support restart
                /etc/init.d/S99openrfid restart
                /etc/init.d/S60klipper restart
        default: disabled

      anycubic_ace_model:
        label: ACE Model
        description: Select which Anycubic ACE model/protocol to use.
        help_url: http://snapmakeru1-extended-firmware.pages.dev/anycubic_ace#ace-model
        if_cmd: test -f /oem/printer_data/config/extended/klipper/ace.cfg
        get_cmd:
          - bash
          - -c
          - |
            cfg=/oem/printer_data/config/extended/klipper/ace.cfg
            test -f "$cfg" || { echo "auto"; exit 0; }
            awk -F': *' '/^device_model:/ {print $2; found=1} END {if (!found) print "auto"}' "$cfg"
        options:
          auto:
            label: Auto Detect
            cmd:
              - bash
              - -ec
              - |
                cfg=/oem/printer_data/config/extended/klipper/ace.cfg
                test -f "$cfg" || { echo "Error: Enable Anycubic ACE first"; exit 0; }
                sed -i 's/^device_model:.*/device_model: auto/' "$cfg"
                echo "ACE Model: Auto Detect. Restarting Klipper..."
                /etc/init.d/S60klipper restart
          ace_pro:
            label: Anycubic ACE Pro
            cmd:
              - bash
              - -ec
              - |
                cfg=/oem/printer_data/config/extended/klipper/ace.cfg
                test -f "$cfg" || { echo "Error: Enable Anycubic ACE first"; exit 0; }
                sed -i 's/^device_model:.*/device_model: ace_pro/' "$cfg"
                echo "ACE Model: Anycubic ACE Pro. Restarting Klipper..."
                /etc/init.d/S60klipper restart
          ace_2_pro:
            label: Anycubic ACE 2 Pro
            cmd:
              - bash
              - -ec
              - |
                cfg=/oem/printer_data/config/extended/klipper/ace.cfg
                test -f "$cfg" || { echo "Error: Enable Anycubic ACE first"; exit 0; }
                sed -i 's/^device_model:.*/device_model: ace_2_pro/' "$cfg"
                echo "ACE Model: Anycubic ACE 2 Pro. Restarting Klipper..."
                /etc/init.d/S60klipper restart
        default: auto

      anycubic_ace_assist_source:
        label: Print-Time Filament Assist
        description: Choose who provides filament assist while printing. ACE loading is always used when Anycubic ACE is enabled.
        help_url: http://snapmakeru1-extended-firmware.pages.dev/anycubic_ace#print-time-filament-assist
        if_cmd: test -f /oem/printer_data/config/extended/klipper/ace.cfg
        get_cmd:
          - bash
          - -c
          - |
            cfg=/oem/printer_data/config/extended/klipper/ace.cfg
            test -f "$cfg" || { echo "snapmaker"; exit 0; }
            awk -F': *' '/^assist_source:/ {print $2; found=1} END {if (!found) print "snapmaker"}' "$cfg"
        options:
          ace:
            label: ACE Feed Assist
            cmd:
              - bash
              - -ec
              - |
                cfg=/oem/printer_data/config/extended/klipper/ace.cfg
                test -f "$cfg" || { echo "Error: Enable Anycubic ACE first"; exit 0; }
                sed -i 's/^assist_source:.*/assist_source: ace/' "$cfg"
                echo "Print-Time Filament Assist: ACE Feed Assist. Restarting Klipper..."
                /etc/init.d/S60klipper restart
          snapmaker:
            label: U1 Feeders
            cmd:
              - bash
              - -ec
              - |
                cfg=/oem/printer_data/config/extended/klipper/ace.cfg
                test -f "$cfg" || { echo "Error: Enable Anycubic ACE first"; exit 0; }
                sed -i 's/^assist_source:.*/assist_source: snapmaker/' "$cfg"
                echo "Print-Time Filament Assist: U1 Feeders. Restarting Klipper..."
                /etc/init.d/S60klipper restart
          "off":
            label: "Off"
            cmd:
              - bash
              - -ec
              - |
                cfg=/oem/printer_data/config/extended/klipper/ace.cfg
                test -f "$cfg" || { echo "Error: Enable Anycubic ACE first"; exit 0; }
                sed -i 's/^assist_source:.*/assist_source: off/' "$cfg"
                echo "Print-Time Filament Assist: Off. Restarting Klipper..."
                /etc/init.d/S60klipper restart
        default: snapmaker

      rfid_hardware:
        options:
          ace:
            label: ACE
            if_cmd: test -f /oem/printer_data/config/extended/klipper/ace.cfg
            cmd:
              - bash
              - -xc
              - |
                /usr/local/bin/extended-config.py add /oem/printer_data/config/extended/extended2.cfg components rfid_hardware ace
                /usr/local/bin/extended-config.py add /oem/printer_data/config/extended/extended2.cfg components rfid_software ace
                cfg=/oem/printer_data/config/extended/klipper/ace.cfg
                test -f "$cfg" && sed -i 's/^rfid_source:.*/rfid_source: ace/' "$cfg" || true
                /etc/init.d/S59rfid-support restart
                /etc/init.d/S99openrfid stop
                /etc/init.d/S60klipper restart

      rfid_software:
        options:
          ace:
            label: ACE
            if_cmd: test -f /oem/printer_data/config/extended/klipper/ace.cfg -a "$(/usr/local/bin/extended-config.py get /oem/printer_data/config/extended/extended2.cfg components rfid_hardware 2>/dev/null || echo snapmaker)" = "ace"
            cmd:
              - bash
              - -xc
              - |
                /usr/local/bin/extended-config.py add /oem/printer_data/config/extended/extended2.cfg components rfid_software ace
                cfg=/oem/printer_data/config/extended/klipper/ace.cfg
                test -f "$cfg" && sed -i 's/^rfid_source:.*/rfid_source: ace/' "$cfg" || true
                /etc/init.d/S99openrfid stop
                /etc/init.d/S60klipper restart
